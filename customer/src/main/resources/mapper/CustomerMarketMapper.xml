<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dili.customer.mapper.CustomerMarketMapper">
  <resultMap id="BaseResultMap" type="com.dili.customer.domain.CustomerMarket">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id"  property="id" />
    <result column="market_id"  property="marketId" />
    <result column="department_id"  property="departmentId" />
    <result column="customer_id"  property="customerId" />
    <result column="owner_id"  property="ownerId" />
    <result column="grade" property="grade" />
    <result column="profession"  property="profession" />
    <result column="profession_name"  property="professionName" />
    <result column="sales_market" property="salesMarket" />
    <result column="alias"  property="alias" />
    <result column="notes"  property="notes" />
    <result column="modifier_id"  property="modifierId" />
    <result column="modify_time"  property="modifyTime" />
  </resultMap>

  <!-- 根据市场ID统计在该市场的客户审核情况 -->
  <select id="statisticsApproval" parameterType="long" resultType="map">
    SELECT
      cm.approval_status AS approvalStatus,
      COUNT(id) AS quantity
    FROM
      customer_market cm
    where cm.market_id = #{marketId}
    GROUP BY
      cm.approval_status
  </select>

</mapper>